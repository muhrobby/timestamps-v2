<script setup lang="ts">
const route = useRoute();

// Breadcrumb items based on current route
const breadcrumbItems = computed(() => {
  const path = route.path;
  const items: { label: string; to?: string }[] = [];

  if (path === "/dashboard") {
    items.push({ label: "Dashboard" });
  } else if (path === "/packing") {
    items.push({ label: "Packing" });
  } else if (path === "/history") {
    items.push({ label: "History" });
  } else if (path === "/users") {
    items.push({ label: "Users" });
  } else if (path === "/stores") {
    items.push({ label: "Stores" });
  } else if (path === "/settings") {
    items.push({ label: "Settings" });
  } else if (path.startsWith("/users/")) {
    items.push({ label: "Users", to: "/users" });
    items.push({ label: "Detail" });
  } else if (path.startsWith("/stores/")) {
    items.push({ label: "Stores", to: "/stores" });
    items.push({ label: "Detail" });
  }

  return items;
});

const isActiveRoute = (path: string) => {
  return route.path === path || route.path.startsWith(path + "/");
};
</script>

<template>
  <div class="min-h-screen bg-gray-50">
    <!-- Mobile sidebar backdrop -->
    <Transition name="fade">
      <div
        v-if="isMobileSidebarOpen"
        class="fixed inset-0 z-20 bg-black/50 lg:hidden"
        @click="isMobileSidebarOpen = false"
      />
    </Transition>

    <!-- Sidebar -->
    <aside
      :class="[
        'fixed left-0 top-0 z-30 h-screen border-r border-gray-200 bg-white transition-transform duration-300 lg:translate-x-0',
        isSidebarOpen ? 'w-64' : 'w-20',
        isMobileSidebarOpen
          ? 'translate-x-0'
          : '-translate-x-full lg:translate-x-0',
      ]"
    >
      <!-- Logo -->
      <div
        class="flex h-16 items-center justify-between border-b border-gray-200 px-4"
      >
        <NuxtLink to="/dashboard" class="flex items-center gap-2">
          <div
            class="flex h-10 w-10 items-center justify-center rounded-lg bg-blue-600 text-white"
          >
            📦
          </div>
          <span v-if="isSidebarOpen" class="font-bold text-gray-900"
            >Packing Docs</span
          >
        </NuxtLink>
        <button
          @click="isSidebarOpen = !isSidebarOpen"
          class="hidden lg:block rounded-lg p-1.5 text-gray-400 hover:bg-gray-100"
        >
          <svg
            class="h-5 w-5"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              :d="
                isSidebarOpen
                  ? 'M11 19l-7-7 7-7m8 14l-7-7 7-7'
                  : 'M13 5l7 7-7 7M5 5l7 7-7 7'
              "
            />
          </svg>
        </button>
      </div>

      <!-- Navigation -->
      <nav class="mt-4 px-3">
        <NuxtLink
          v-for="item in navigation"
          :key="item.path"
          :to="item.path"
          :class="[
            'sidebar-item mb-1',
            { 'sidebar-item-active': isActiveRoute(item.path) },
          ]"
          @click="isMobileSidebarOpen = false"
        >
          <svg
            class="h-5 w-5 flex-shrink-0"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              :d="item.icon"
            />
          </svg>
          <span v-if="isSidebarOpen">{{ item.name }}</span>
        </NuxtLink>
      </nav>

      <!-- User Info -->
      <div
        class="absolute bottom-0 left-0 right-0 border-t border-gray-200 p-4"
      >
        <div class="flex items-center gap-3">
          <div
            class="flex h-10 w-10 items-center justify-center rounded-full bg-blue-100 text-blue-600"
          >
            {{ user?.name?.charAt(0).toUpperCase() }}
          </div>
          <div v-if="isSidebarOpen" class="flex-1 truncate">
            <p class="text-sm font-medium text-gray-900 truncate">
              {{ user?.name }}
            </p>
            <p class="text-xs text-gray-500">{{ user?.role }}</p>
          </div>
          <button
            v-if="isSidebarOpen"
            @click="logout"
            class="rounded-lg p-1.5 text-gray-400 hover:bg-red-50 hover:text-red-500"
            title="Logout"
          >
            <svg
              class="h-5 w-5"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"
              />
            </svg>
          </button>
        </div>
      </div>
    </aside>

    <!-- Main Content -->
    <div
      :class="[
        'transition-all duration-300',
        isSidebarOpen ? 'lg:ml-64' : 'lg:ml-20',
      ]"
    >
      <!-- Top Bar -->
      <header
        class="sticky top-0 z-10 flex h-16 items-center justify-between border-b border-gray-200 bg-white px-4 lg:px-6"
      >
        <button
          @click="isMobileSidebarOpen = true"
          class="rounded-lg p-2 text-gray-600 hover:bg-gray-100 lg:hidden"
        >
          <svg
            class="h-6 w-6"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M4 6h16M4 12h16M4 18h16"
            />
          </svg>
        </button>

        <div class="flex items-center gap-4">
          <span class="text-sm text-gray-500">{{ user?.storeName }}</span>
        </div>
      </header>

      <!-- Page Content -->
      <main class="p-4 lg:p-6">
        <slot />
      </main>
    </div>

    <!-- Upload Progress Widget -->
    <PackingUploadProgressWidget />
  </div>
</template>

<style scoped>
.fade-enter-active,
.fade-leave-active {
  transition: opacity 0.2s;
}

.fade-enter-from,
.fade-leave-to {
  opacity: 0;
}
</style>
